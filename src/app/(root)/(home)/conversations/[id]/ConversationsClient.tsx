'use client';

import { NextPage } from 'next';

interface Props {}

const ConversationsClient: NextPage<Props> = ({}) => {
  return (
    <div>
      <p>Let's start chats</p>
    </div>
  );
};

export default ConversationsClient;
